{
  "id": "001",
  "title": "GET /meta advertises the version, capabilities and keepalive",
  "spec": "PROTOCOL.md#2-transport",
  "notes": "The `server` member is free-form, so it is a binding: every other byte is exact. The reference server writes this body with `serde_json::to_string`, which is the canonical form of CANONICAL.md. A path that is not `/meta` answers 404.",
  "steps": [
    {
      "op": "http",
      "target": "/meta"
    },
    {
      "op": "expectStatus",
      "status": 200
    },
    {
      "op": "expectBody",
      "text": "{\"capabilities\":[\"y-protocols/1\",\"awareness\",\"open-document-set\",\"host-reclaim\"],\"keepalive\":{\"awareness_expire_ms\":30000,\"awareness_renew_ms\":15000,\"ping_interval_ms\":30000},\"roles\":[\"host\",\"guest\"],\"server\":\"$server\",\"wire_versions\":[\"selvage/1\"]}"
    },
    {
      "op": "http",
      "target": "/nope"
    },
    {
      "op": "expectStatus",
      "status": 404
    }
  ],
  "selvage": "selvage/1",
  "canonical": "SJ-C/1"
}
